import React from "react";
import ReactDOM from "react-dom/client";
import CalcettoApp from "./CalcettoApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CalcettoApp />
  </React.StrictMode>
);
